# Qwen Adapter

> Load `universal-core.md` first. This file only contains Qwen-specific overrides.

## Tools available
- File system tools
- Bash for commands

## Behavior notes
- Use QWEN.md in the project root as your project-specific context
- Playwright browser tools: run `npx @playwright/mcp@latest` manually if needed
- Prefer smaller, more focused tasks — Qwen handles tight scope better than broad exploration
